# Core
torch>=2.0
torchvision>=0.15
numpy>=1.24
opencv-python>=4.7
scipy>=1.10
PyYAML>=6.0
tqdm>=4.65

# Landmark GT (mediapipe facemesh)
mediapipe>=0.10

# Metrics / viz
scikit-learn>=1.3
matplotlib>=3.7

# Notebooks
jupyter
nbformat>=5.9
nbconvert>=7.0

# ORFormer vendor (외부): /data/shared/orformer/vendor 를 PYTHONPATH 에 추가
# torchlm 등 vendor 내부 의존은 해당 경로 참조
