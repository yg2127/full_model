# byte-compiled
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# 대용량 학습 산출물 / 데이터 (README 경로 참조)
*.pt
*.pth
*.ckpt
*.npz
*.npy
*.mp4
*.avi
*.wav
*.mp3

# full_system 런타임 산출물: 대용량만 차단(작은 jsonl/csv/png 샘플은 추적)
full_system/Model/
full_system/outputs/**/warning_predictions.jsonl
full_system/outputs/**/*.mp4
full_system/experiments/**/runs/
full_system/experiments/**/run_all.log

# comparison/ · ablation/ 실험 스냅샷: 코드+작은 결과만 추적, 대용량 산출물 차단
**/artifacts/
**/artifacts_*/
**/predictions/
**/bootstrap_results_*/raw/
**/*clip_predictions*.csv
**/*_predictions.csv
**/*confusion_epoch_*.csv
*.log

# 결과/캐시 디렉토리
# results: 대용량만 차단(핵심 결과 CSV/json 은 추적)
results/**/*.pt
results/**/clip_manifest_*.json
artifacts/
runs/
logs/
_splits*/
hgnet_fixedmask_ft/
wandb/

# OS
.DS_Store


# 모델 가중치(바이너리)만 미추적 — models/ 내 .md(README/PROVENANCE)는 추적
models/**/*.pt
models/**/*.tflite
*.tflite

# 발표 원본(편집용, 대용량) — 제외
docs/*.pptx
